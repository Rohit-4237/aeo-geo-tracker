# ─── Platform-hosted mode keys (fill these in if YOU bear the API cost) ───────
# Leave blank to require users to enter their own keys via the Settings page.
PLATFORM_OPENAI_API_KEY=
PLATFORM_GEMINI_API_KEY=
PLATFORM_PERPLEXITY_API_KEY=

# ─── App settings ─────────────────────────────────────────────────────────────
DATABASE_URL=sqlite+aiosqlite:///./data/aeo_tracker.db
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
